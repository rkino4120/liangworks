# microCMSの設定例
# 実際の値は環境変数ファイル(.env)に設定してください

# microCMSサービスドメイン（サーバーサイド用）
MICROCMS_SERVICE_DOMAIN=your-service-domain

# microCMSのAPIキー（サーバーサイド用）
MICROCMS_API_KEY=your-api-key

# microCMSサービスドメイン（クライアントサイド用）
# 注意: VITE_プレフィックス付きの環境変数はクライアントに公開されます
VITE_MICROCMS_SERVICE_DOMAIN=your-service-domain

# microCMSのAPIキー（クライアントサイド用）
# 注意: 読み取り専用のAPIキーを使用してください（公開されるため）
VITE_MICROCMS_API_KEY=your-readonly-api-key

# 設定手順:
# 1. microCMSでアカウントを作成
# 2. サービスを作成（例: blog-service）
# 3. APIを作成（例: blogs）
# 4. コンテンツモデルを設定:
#    - title: テキストフィールド
#    - content: リッチエディタ
#    - eyecatch: 画像フィールド（任意）
#    - category: 参照フィールド（任意）
#    - tags: 複数参照フィールド（任意）
# 5. APIキーを取得してこのファイルに設定
# 6. src/lib/microcms.ts の endpoint を実際のAPI名に変更
